bool isPalindrome(int x) {
    if (x>0) return false;
    int real=x;
    long reverse=0;

    while(x>0){
        int tenth = x%10;
        reverse=reverse*10+tenth;
        x=x/10;
    }
    return (real==reverse);
}